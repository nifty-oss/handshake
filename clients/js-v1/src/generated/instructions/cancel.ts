/**
 * This code was AUTOGENERATED using the kinobi library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun kinobi to update it.
 *
 * @see https://github.com/metaplex-foundation/kinobi
 */

import {
  Context,
  Pda,
  PublicKey,
  Signer,
  TransactionBuilder,
  transactionBuilder,
} from '@metaplex-foundation/umi';
import {
  Serializer,
  mapSerializer,
  struct,
  u8,
} from '@metaplex-foundation/umi/serializers';
import { findHandshakePda } from '../accounts';
import {
  ResolvedAccount,
  ResolvedAccountsWithIndices,
  expectPublicKey,
  getAccountMetasAndSigners,
} from '../shared';

// Accounts.
export type CancelInstructionAccounts = {
  /** Handshake account */
  handshake?: PublicKey | Pda;
  /** Asset account */
  asset: PublicKey | Pda;
  /** The current authority of the asset (source) */
  authority?: Signer;
  /** The account to receive the funds of storage fees */
  recipient: PublicKey | Pda;
  /** The nifty asset program */
  niftyAssetProgram?: PublicKey | Pda;
};

// Data.
export type CancelInstructionData = { discriminator: number };

export type CancelInstructionDataArgs = {};

export function getCancelInstructionDataSerializer(): Serializer<
  CancelInstructionDataArgs,
  CancelInstructionData
> {
  return mapSerializer<CancelInstructionDataArgs, any, CancelInstructionData>(
    struct<CancelInstructionData>([['discriminator', u8()]], {
      description: 'CancelInstructionData',
    }),
    (value) => ({ ...value, discriminator: 1 })
  ) as Serializer<CancelInstructionDataArgs, CancelInstructionData>;
}

// Instruction.
export function cancel(
  context: Pick<Context, 'eddsa' | 'identity' | 'programs'>,
  input: CancelInstructionAccounts
): TransactionBuilder {
  // Program ID.
  const programId = context.programs.getPublicKey(
    'handshake',
    'HndshkSZWu1f4qdwyACwsP8YoBxvCzb7NNVB1Aj3w656'
  );

  // Accounts.
  const resolvedAccounts = {
    handshake: {
      index: 0,
      isWritable: true as boolean,
      value: input.handshake ?? null,
    },
    asset: {
      index: 1,
      isWritable: true as boolean,
      value: input.asset ?? null,
    },
    authority: {
      index: 2,
      isWritable: false as boolean,
      value: input.authority ?? null,
    },
    recipient: {
      index: 3,
      isWritable: true as boolean,
      value: input.recipient ?? null,
    },
    niftyAssetProgram: {
      index: 4,
      isWritable: false as boolean,
      value: input.niftyAssetProgram ?? null,
    },
  } satisfies ResolvedAccountsWithIndices;

  // Default values.
  if (!resolvedAccounts.handshake.value) {
    resolvedAccounts.handshake.value = findHandshakePda(context, {
      asset: expectPublicKey(resolvedAccounts.asset.value),
    });
  }
  if (!resolvedAccounts.authority.value) {
    resolvedAccounts.authority.value = context.identity;
  }
  if (!resolvedAccounts.niftyAssetProgram.value) {
    resolvedAccounts.niftyAssetProgram.value = context.programs.getPublicKey(
      'niftyAsset',
      'AssetGtQBTSgm5s91d1RAQod5JmaZiJDxqsgtqrZud73'
    );
    resolvedAccounts.niftyAssetProgram.isWritable = false;
  }

  // Accounts in order.
  const orderedAccounts: ResolvedAccount[] = Object.values(
    resolvedAccounts
  ).sort((a, b) => a.index - b.index);

  // Keys and Signers.
  const [keys, signers] = getAccountMetasAndSigners(
    orderedAccounts,
    'programId',
    programId
  );

  // Data.
  const data = getCancelInstructionDataSerializer().serialize({});

  // Bytes Created On Chain.
  const bytesCreatedOnChain = 0;

  return transactionBuilder([
    { instruction: { keys, programId, data }, signers, bytesCreatedOnChain },
  ]);
}
